{
  "_description": "Global fallback baseline. Used for families with no per-family baseline file. Update or create a per-family file (e.g. words.json) when a family's expected counts are known.",
  "_note": "Per-family baselines take precedence: tests/baselines/<family>.json (e.g. words.json). This file is only used when no family-specific file exists.",
  "_updated": "2026-05-08",
  "defect_counts": {}
}
